function changeBackground() {
    var element = document.body;
    element.classList.toggle("dark-mode");
    var textButton = document.getElementsByClassName("botao").value;
    console.log(textButton);
}